
function dydt = odefun(t, y)
    % Extract variables from the y vector
    xm = y(1);
    p = y(2);
    q = y(3);
    
    global R C 

    m = 0.3;
    I = m;


    % Define the system of ODEs
    xmdot = 
    pdot  = 
    qdot  = 

    % Pack the derivatives into a column vector
    dydt = [xmdot x2dot; v2dot];
end
